<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Product Show Page</title>
</head>

<body>
    <nav>
        <a href="/products">Back to Products Index</a>
    </nav>
    <h1><%=product.name%></h1>
    dscription: <%= product.description%>
    image <%= product.img%>
    price: <%= product.price%>
    qty: <%= product.qty%>
    <br />
    <% for(let i=0;i<product.qty.length;i++) { %>

        <span onclick="getId('<%=product.qty[i].id%>')"><%=product.qty[i].name%></span>
        
        <% } %>
</body>

</html>